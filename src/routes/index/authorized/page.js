import React from "react";

const Page = data => {
  return (
    <div
      className="col s8"
      style={{
        padding: 0,
        height: "100%",
        display: "flex",
        alignItems: "center",
        justifyContent: "center",
        fontStyle: "italic",
        color: "#ababab"
      }}
    >
      <h5>Выберите чат</h5>
    </div>
  );
};

export default Page;
